int main()
{
    int x = 9;
    int y = 20;
    int z;
    if (x < y)
    {
        printf("x < y\n");
    }
    if (x < 10)
    {
        z = x + 100;
        printf("peelable start1!\n");
        x = z + 4;
        z = y + 3;
        printf("y = %d\n", y);
        ++x;
        while (x < 10)
        {
            x = z + 4;
            z = y + 3;
            printf("y = %d\n", y);
            ++x;
        }
    }
    else
    {
        z = x + 100;
        printf("peelable start1!\n");
    }
    if (x < 20)
    {
        printf("peelable start2!\n");
        x = z + 10;
        z = y + 23;
        printf("y2 = %d\n", y);
        ++x;
        while (x < 20)
        {
            x = z + 10;
            z = y + 23;
            printf("y2 = %d\n", y);
            ++x;
        }
    }
    else
    {
        printf("peelable start2!\n");
    }
    return 0;
}
